sign in
  (Username, Password, e-mail)
Download the Code
	(Code->Download Zip) in hard disc C




Open Anaconda Prompt (as admin)
	git pull (it wil go to the same file as the code)

After the use of the code:
	In the prompt: 
	Pick the location where the file is
	->git add project1.py
	->git commit -m "write message"
	->git push origin master
	asks for username
	asks for password (what you type will be hidden)
	